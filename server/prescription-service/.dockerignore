# Bỏ qua thư mục chứa các thư viện đã cài, chúng sẽ được cài lại bên trong container
node_modules

# Bỏ qua file biến môi trường, chúng ta sẽ nạp nó vào lúc chạy thay vì build vào image
.env

# Bỏ qua các file log
npm-debug.log